name: Testes Multi-SO

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    name: Testar em ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.11']
        include:
          - os: ubuntu-latest
            python-version: '3.8'
          - os: windows-latest
            python-version: '3.8'
          - os: macos-latest
            python-version: '3.8'
    
    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4
      
      - name: 🐍 Configurar Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        shell: bash
      
      - name: 🔍 Informações do ambiente
        run: |
          echo "🎯 Sistema: ${{ matrix.os }}"
          echo "🐍 Python: ${{ matrix.python-version }}"
          echo "📂 Diretório: $(pwd)"
          echo "📋 Conteúdo do src/:"
          ls -la src/ || echo "src/ não encontrado"
        shell: bash
      
      - name: 🧪 Executar testes unitários
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: |
          echo "==================================="
          echo "🧪 TESTANDO EM ${{ matrix.os }}"
          echo "==================================="
          export PYTHONPATH="${PYTHONPATH}:$(pwd)"
          python -m unittest discover -v tests/
        shell: bash
      
      - name: ✅ Validar saída esperada
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: |
          python -c "
          import sys
          import platform
          sys.path.insert(0, '.')
          from src.sistema_info import SistemaInfo

          info = SistemaInfo.get_info_completa()
          sistema_real = platform.system()

          assert info['sistema'] == sistema_real, f'Sistema incorreto: {info[\"sistema\"]} != {sistema_real}'
          print('✅ Validação básica passou!')
          "
        shell: bash
      
      - name: 📤 Upload do relatório
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-${{ matrix.os }}-py${{ matrix.python-version }}
          path: relatorio_*.json

  summary:
    name: 📋 Resumo dos testes
    needs: test
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 📥 Download todos os relatórios
        uses: actions/download-artifact@v4
        with:
          path: relatorios
      
      - name: 📊 Exibir resumo
        run: |
          echo "==================================="
          echo "📋 RESUMO FINAL DOS TESTES"
          echo "==================================="
          echo "✅ Pipeline executada com sucesso!"
          echo ""
          echo "📊 Resultados por SO:"
          echo "  • Ubuntu (3.8, 3.11)"
          echo "  • Windows (3.8, 3.11)"
          echo "  • macOS (3.8, 3.11)"
          echo ""
          echo "📁 Relatórios disponíveis nos artifacts"
        shell: bash